import {useEffect,useState} from'react';import PageHeader from'./PageHeader';import SearchBar from'./SearchBar';import DataTable from'./DataTable';import ErrorMessage from'./ErrorMessage';import{asArray,getApiMessage,money}from'../../utils/data';
export default function ModuleList({title,description,loader,columns,action}){const[rows,setRows]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[q,setQ]=useState('');const load=async()=>{setLoading(true);try{setRows(asArray((await loader()).data));setError('')}catch(e){setError(getApiMessage(e))}finally{setLoading(false)}};useEffect(()=>{load()},[]);const filtered=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q.toLowerCase()));return <><PageHeader title={title} description={description} action={action}/><div className="toolbar"><SearchBar value={q} onChange={setQ}/></div>{error&&<ErrorMessage message={error} onRetry={load}/>}<DataTable rows={filtered} loading={loading} columns={columns.map(c=>({...c,render:c.render||((r)=>['amount','total','balance','price'].includes(c.key)?money(r[c.key]):r[c.key]??'—')}))}/></>}
