export default function FormSelect({ label, options = [], placeholder = 'Select…', error, required, ...props }) { return <label className="field"><span>{label}{required && <b> *</b>}</span><select {...props} required={required}><option value="">{placeholder}</option>{options.map((option) => <option key={option.value ?? option.id} value={option.value ?? option.id}>{option.label ?? option.name}</option>)}</select>{error && <small className="field__error">{error}</small>}</label>; }
