import ModuleList from'../../components/common/ModuleList';import{reconciliationService}from'../../services/reconciliationService';export default function ReconciliationHistory(){return <ModuleList title="Reconciliation History" description="Your previous reconciliation submissions." loader={reconciliationService.history} columns={[{key:'id',label:'ID'},{key:'status',label:'Status'},{key:'submitted_cash',label:'Submitted Cash'},{key:'created_at',label:'Date'}]}/>}
