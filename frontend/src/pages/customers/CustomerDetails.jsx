import{useLocation}from'react-router-dom';export default function CustomerDetails(){const{state}=useLocation();return <div className="form-card"><h2>Customer Details</h2><p>{state?'Details received from the navigation context.':'Use the customer list to select a customer. The backend contract only exposes create and my-customers endpoints here.'}</p>{state&&<pre className="json-view">{JSON.stringify(state,null,2)}</pre>}</div>}
