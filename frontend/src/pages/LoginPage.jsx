import { useState } from 'react'; import { Navigate,useLocation,useNavigate } from 'react-router-dom'; import FormInput from '../components/common/FormInput'; import ErrorMessage from '../components/common/ErrorMessage'; import ThemeToggle from '../components/common/ThemeToggle'; import { useAuth } from '../context/AuthContext'; import { errorMessage } from '../utils/data';
export default function LoginPage(){const{login,user}=useAuth();const nav=useNavigate();const location=useLocation();const[form,setForm]=useState({email:'',password:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);if(user)return <Navigate to="/dashboard" replace/>;const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{await login(form);nav(location.state?.from?.pathname||'/dashboard',{replace:true})}catch(err){setError(errorMessage(err,'Unable to sign in. Check your email and password.'))}finally{setLoading(false)}};return <main className="login-page"><section className="login-story"><div className="login-brand"><span>S</span><strong>ShopFlow</strong></div><div><span className="eyebrow">Complete business control</span><h1>From supplier stock to every truck sale.</h1><p>One operational workspace for inventory, distribution, collections, reconciliation, and management insight.</p></div><div className="login-flow">Supplier <i>→</i> Store <i>→</i> Truck <i>→</i> Customer</div><small>Secure access for Super Admin, Admin, Storekeeper, and Salesperson.</small></section><section className="login-panel"><div className="login-panel__top"><span>Shop Management System</span><ThemeToggle/></div><form onSubmit={submit}><span className="eyebrow">Welcome back</span><h2>Sign in to your workspace</h2><p>Use the account created for you by the Super Admin.</p><ErrorMessage message={error}/><FormInput label="Email address" type="email" autoComplete="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><FormInput label="Password" type="password" autoComplete="current-password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><button className="btn btn--primary btn--wide" disabled={loading}>{loading?'Signing in…':'Sign in securely →'}</button></form><footer>Shop Management System © {new Date().getFullYear()}</footer></section></main>}
