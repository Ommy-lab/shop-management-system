import EntityForm from '../../components/common/EntityForm'; import truckService from '../../services/truckService';
const fields=[{name:'name',label:'Truck name',required:true},{name:'registration_number',label:'Registration / plate number',required:true},{name:'capacity',label:'Capacity',type:'number',min:'0'},{name:'status',label:'Status',type:'select',options:[{value:'ACTIVE',label:'Active'},{value:'INACTIVE',label:'Inactive'},{value:'MAINTENANCE',label:'Maintenance'}]}];
export default function TruckForm(){return <EntityForm title="Truck" service={truckService} fields={fields} backTo="/trucks" dataKey="truck" initial={{status:'ACTIVE'}}/>}
