import ResourceForm from '../../components/common/ResourceForm';import service from '../../services/truckService';export default function Form(){return <ResourceForm title="Truck" service={service} fields={[{name:'registration_number',label:'Registration Number',type:'text',required:true},{name:'description',label:'Description',type:'textarea',required:false},{name:'status',label:'Status',type:'select',required:true,options:['ACTIVE','INACTIVE']}]} path="/trucks"/>}
