import ModuleList from '../../components/common/ModuleList';import{truckLoadService}from'../../services/truckLoadService';export default function TruckLoads(){return <ModuleList title="Truck Loads" description="Store-to-truck loading records." loader={truckLoadService.list} columns={[{key:'id',label:'ID'},{key:'truck_id',label:'Truck'},{key:'created_at',label:'Date'},{key:'status',label:'Status'}]}/>}
