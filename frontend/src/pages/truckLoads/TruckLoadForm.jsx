import ResourceForm from '../../components/common/ResourceForm';import{truckLoadService}from'../../services/truckLoadService';export default function TruckLoadForm(){return <ResourceForm title="Truck Load" service={truckLoadService} fields={[{name:'truck_id',label:'Truck ID',required:true},{name:'product_id',label:'Product ID',required:true},{name:'quantity',label:'Quantity',type:'number',required:true}]} path="/truck-loads"/>}
