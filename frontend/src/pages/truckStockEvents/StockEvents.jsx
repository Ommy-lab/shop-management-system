import ModuleList from '../../components/common/ModuleList';import service from '../../services/truckStockEventService';export default function StockEvents(){return <ModuleList title="Stock Events" description="Truck stock event history." loader={service.listMine} columns={[{key:'id',label:'ID'},{key:'event_type',label:'Event Type'},{key:'date',label:'Date'},{key:'notes',label:'Notes'}]}/>;}
