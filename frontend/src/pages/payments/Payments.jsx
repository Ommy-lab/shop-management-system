import{useState}from'react';import{useNavigate}from'react-router-dom';import PageHeader from'../../components/common/PageHeader';import FormInput from'../../components/common/FormInput';export default function Payments(){const[id,setId]=useState('');const nav=useNavigate();return <><PageHeader title="Payments" description="Open payment history or record a payment for a sale."/><div className="form-card"><FormInput label="Sale ID" value={id} onChange={e=>setId(e.target.value)} required/><div className="form-actions"><button className="btn btn--secondary" disabled={!id} onClick={()=>nav(`/sales/${id}/payments`)}>View Payments</button><button className="btn btn--primary" disabled={!id} onClick={()=>nav(`/sales/${id}/payment`)}>Record Payment</button></div></div></>}
