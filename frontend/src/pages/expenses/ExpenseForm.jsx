import ResourceForm from '../../components/common/ResourceForm';import{expenseService}from'../../services/expenseService';export default function ExpenseForm(){return <ResourceForm title="Expense" service={expenseService} fields={[{name:'category',label:'Category',required:true},{name:'amount',label:'Amount',type:'number',required:true},{name:'description',label:'Description',type:'textarea'},{name:'expense_date',label:'Expense Date',type:'date',required:true}]} path="/expenses"/>}
