import{useState}from'react';import{useParams,useNavigate}from'react-router-dom';import FormInput from'../../components/common/FormInput';import PageHeader from'../../components/common/PageHeader';import{userService}from'../../services/userService';export default function ResetPassword(){const{id}=useParams();const nav=useNavigate();const[p,setP]=useState('');const save=async e=>{e.preventDefault();await userService.resetPassword(id,{password:p});nav('/users')};return <><PageHeader title="Reset Password"/><form className="form-card" onSubmit={save}><FormInput label="New Password" type="password" value={p} onChange={e=>setP(e.target.value)} required/><button className="btn btn--primary">Reset Password</button></form></>}
