import{useState}from'react';import{useParams,useNavigate}from'react-router-dom';import FormInput from'../../components/common/FormInput';import PageHeader from'../../components/common/PageHeader';import{userService}from'../../services/userService';export default function AssignTruck(){const{id}=useParams();const nav=useNavigate();const[t,setT]=useState('');const save=async e=>{e.preventDefault();await userService.assignTruck(id,{truck_id:t});nav('/users')};return <><PageHeader title="Assign Truck"/><form className="form-card" onSubmit={save}><FormInput label="Truck ID" value={t} onChange={e=>setT(e.target.value)} required/><button className="btn btn--primary">Assign</button></form></>}
