import { useAuth } from '../../context/AuthContext'; import SuperAdminDashboard from './SuperAdminDashboard'; import AdminDashboard from './AdminDashboard'; import StorekeeperDashboard from './StorekeeperDashboard'; import SalespersonDashboard from './SalespersonDashboard'; import { ROLES } from '../../utils/roles';
export default function DashboardRouter(){const{user}=useAuth();const View={ [ROLES.SUPER_ADMIN]:SuperAdminDashboard,[ROLES.ADMIN]:AdminDashboard,[ROLES.STOREKEEPER]:StorekeeperDashboard,[ROLES.SALESPERSON]:SalespersonDashboard }[user?.role];return View?<View/>:null}
