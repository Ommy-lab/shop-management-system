import EntityForm from '../../components/common/EntityForm'; import supplierService from '../../services/supplierService';
const fields=[{name:'name',label:'Supplier name',required:true},{name:'phone',label:'Phone',type:'tel',required:true},{name:'location',label:'Location',required:true},{name:'status',label:'Status',type:'select',options:[{value:'ACTIVE',label:'Active'},{value:'INACTIVE',label:'Inactive'}]}];
export default function SupplierForm(){return <EntityForm title="Supplier" service={supplierService} fields={fields} backTo="/suppliers" dataKey="supplier" initial={{status:'ACTIVE'}}/>}
