import{useEffect,useState}from'react';import PageHeader from'../../components/common/PageHeader';import DataTable from'../../components/common/DataTable';import{inventoryService}from'../../services/inventoryService';import{asArray}from'../../utils/data';export default function StockMovements(){const[d,setD]=useState([]);useEffect(()=>{inventoryService.movements().then(r=>setD(asArray(r.data)))},[]);return <><PageHeader title="Stock Movements" description="Track stock changes and their references."/><DataTable rows={d} columns={['product','movement_type','quantity','reference','notes','date'].map(k=>({key:k,label:k.replaceAll('_',' ')}))}/></>}
